<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.PracticumAndroidDiploma" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Основной акцентный цвет: курсор при вводе, цвет кнопок -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Цвет активити по умолчанию -->
        <item name="android:background">@color/black</item>
        <item name="colorBackgroundPrimary">@color/black</item>
        <item name="colorOnBackgroundPrimary">@color/white</item>
        <item name="colorTextField">@color/gray</item>
        <item name="colorHintTextField">@color/white</item>
    </style>
</resources>
