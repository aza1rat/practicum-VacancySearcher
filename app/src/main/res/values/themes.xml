<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.PracticumAndroidDiploma" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Основной акцентный цвет: курсор при вводе, цвет кнопок, плавающая подсказка TextField -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Цвет активити по умолчанию -->
        <item name="android:background">@color/white</item>
        <item name="colorBackgroundPrimary">@color/white</item>
        <item name="colorOnBackgroundPrimary">@color/black</item>
        <item name="colorTextField">@color/light_gray</item>
        <item name="colorHintTextField">@color/gray</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.PracticumAndroidDiploma" parent="Base.Theme.PracticumAndroidDiploma" />
</resources>
